enum MouseButtonName {
  spMouseLeft,
  spMouseMiddle,
  spMouseRight,
}

enum ScrollName {
  // Negative delta.
  spScrollUp = 'spScrollUp',
  // Positive delta.
  spScrollDown = 'spScrollDown',
}

export {
  MouseButtonName,
  ScrollName,
}
